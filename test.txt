class MinhaClasse {

    int x = 10;
    boolean ativo = TRUE;
    string nome = "Matheus";

    function soma {
        x = x + 1;

        if (x > 5) {
            y = y + 2;
        } else {
            y = y - 1;
        }

        while (x < 20) {
            x = x + 1;
        }

        for (x = 0; x < 5; x = x + 1) {
            nome = "looping";
        }
    }

    function teste {
        ativo = FALSE;

        if (ativo == FALSE) {
            x = x - 1;
        } else {
            x = x + 1;
        }
    }
}